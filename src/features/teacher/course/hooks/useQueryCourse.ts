/**
 * 课程查询 hooks
 * TODO: 等待后端添加课程管理 API
 * 需要的接口：
 * - GET /api/teacher/courses - 查询课程列表
 * - GET /api/teacher/courses/{courseId} - 查询课程详情
 */

/*
export function useQueryCourseAll() {
  // 示例实现，等待后端API
  return useQuery({
    queryKey: ["courses-all"],
    queryFn: () => {
      // TODO: 调用后端API
    },
  });
}

export function useQueryCourseById(courseId: string) {
  // 示例实现，等待后端API
  return useQuery({
    queryKey: ["courses", courseId],
    queryFn: () => {
      // TODO: 调用后端API
    },
  });
}
*/
