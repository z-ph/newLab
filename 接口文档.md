# 实验签到管理系统 - 接口文档

## 项目概述

这是一个基于 Spring Boot 的高校实验课签到管理系统，主要用于管理学生上实验课时的签到、实验流程、数据采集和成绩管理。

---

## 目录

- [一、用户认证模块](#一用户认证模块)
- [二、班级管理模块](#二班级管理模块教师端)
- [三、签到管理模块](#三签到管理模块)
- [四、实验管理模块](#四实验管理模块待实现)
- [五、数据采集模块](#五数据采集模块待实现)
- [六、题目和标签管理模块](#六题目和标签管理模块待实现)
- [七、课程管理模块](#七课程管理模块待实现)
- [八、成绩管理模块](#八成绩管理模块待实现)
- [九、数据导入导出模块](#九数据导入导出模块)

---

## 一、用户认证模块

### 1. 用户登录

| 接口路径 | 请求方式 | 说明 | 权限要求 |
|---------|---------|------|---------|
| `/api/auth/login` | POST | 用户名密码登录，返回 JWT Token | 无 |

**请求参数：**
```json
{
  "username": "2021001",
  "password": "123456",
  "code": "微信授权码（可选）"
}
```

**响应示例：**
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "user": {
      "id": 1,
      "username": "2021001",
      "name": "张三",
      "role": "STUDENT"
    }
  }
}
```

---

### 2. 微信登录

| 接口路径 | 请求方式 | 说明 | 权限要求 |
|---------|---------|------|---------|
| `/api/auth/login-by-openid` | POST | 通过微信 openid 直接登录 | 无 |
| `/api/auth/login-by-code` | POST | 通过微信授权 code 登录 | 无 |
| `/api/auth/wechat-status/{username}` | GET | 查询用户微信绑定状态 | 无 |
| `/api/auth/unbind-wechat` | POST | 学生解绑微信 | STUDENT |

---

### 3. 密码管理

| 接口路径 | 请求方式 | 说明 | 权限要求 |
|---------|---------|------|---------|
| `/api/auth/set-password` | POST | 用户设置/修改密码（6位数字） | 无 |
| `/api/auth/reset-password` | POST | 管理员重置用户密码 | ADMIN |

**设置密码请求参数：**
```json
{
  "username": "2021001",
  "newPassword": "123456",
  "confirmPassword": "123456"
}
```

**重置密码请求参数：**
```json
["2021001", "2021002", "2021003"]
```

---

### 4. 用户状态查询

| 接口路径 | 请求方式 | 说明 | 权限要求 |
|---------|---------|------|---------|
| `/api/auth/check-user-status/{username}` | GET | 检查用户是否存在、是否已设置密码 | 无 |
| `/api/auth/debug-user/{username}` | GET | 调试接口：查询用户详细信息 | 无 |
| `/api/auth/test-db` | GET | 数据库连接测试 | 无 |

---

## 二、班级管理模块（教师端）

### 1. 班级 CRUD

| 接口路径 | 请求方式 | 说明 | 权限要求 |
|---------|---------|------|---------|
| `/api/teacher/class` | POST | 创建班级 | TEACHER |
| `/api/teacher/class/{id}` | GET | 根据 ID 查询班级（含实验信息） | TEACHER |
| `/api/teacher/class/code/{classCode}` | GET | 根据班级代码查询班级 | TEACHER |
| `/api/teacher/class/{id}` | PUT | 更新班级信息 | TEACHER |
| `/api/teacher/class/{id}` | DELETE | 删除班级 | TEACHER |
| `/api/teacher/class/query` | POST | 分页查询班级列表 | TEACHER |

**创建班级请求参数：**
```json
{
  "classCode": "CS202301",
  "className": "计算机科学与技术1班"
}
```

**查询班级请求参数：**
```json
{
  "classCode": "CS202301",
  "className": "计算机",
  "creator": "T001",
  "page": 1,
  "pageSize": 10
}
```

---

### 2. 批量操作

| 接口路径 | 请求方式 | 说明 | 权限要求 |
|---------|---------|------|---------|
| `/api/teacher/class/batch` | POST | 批量添加班级 | TEACHER |

**批量添加班级请求参数：**
```json
{
  "classes": [
    {
      "classCode": "CS202302",
      "className": "计算机科学与技术2班"
    },
    {
      "classCode": "CS202303",
      "className": "计算机科学与技术3班"
    }
  ]
}
```

---

### 3. 学生绑定管理

| 接口路径 | 请求方式 | 说明 | 权限要求 |
|---------|---------|------|---------|
| `/api/teacher/class/{classCode}/students` | GET | 查询班级所有学生 | TEACHER |
| `/api/teacher/class/{classCode}/bind-students` | POST | 批量绑定学生到班级 | TEACHER |
| `/api/teacher/class/{classCode}/unbind-students` | POST | 批量解绑学生 | TEACHER |

**批量绑定学生请求参数：**
```json
{
  "classCode": "CS202301",
  "studentUsernames": ["2021001", "2021002", "2021003"]
}
```

**批量解绑学生请求参数：**
```json
["2021001", "2021002", "2021003"]
```

---

### 4. 班级实验绑定

| 接口路径 | 请求方式 | 说明 | 权限要求 |
|---------|---------|------|---------|
| `/api/teacher/class/bind-experiment` | POST | 批量绑定班级到实验 | TEACHER |
| `/api/teacher/class/unbind-experiment/{experimentId}` | POST | 批量解绑班级 | TEACHER |

**批量绑定班级到实验请求参数：**
```json
{
  "experimentId": "EXP001",
  "classCodes": ["CS202301", "CS202302", "CS202303"]
}
```

---

## 三、签到管理模块

### 1. 教师端签到管理

| 接口路径 | 请求方式 | 说明 | 权限要求 |
|---------|---------|------|---------|
| `/api/teacher/attendance/list` | GET | 查询指定班级实验的签到情况 | TEACHER |
| `/api/teacher/attendance/update` | POST | 修改签到状态 | TEACHER |
| `/api/teacher/attendance/records` | GET | 根据课程和实验查询签到记录 | TEACHER |
| `/api/teacher/attendance/count` | GET | 统计已签到学生数量 | TEACHER |

**查询签到列表参数：**
```
classExperimentId=1
```

**修改签到状态请求参数：**
```json
{
  "studentUsername": "2021001",
  "classExperimentId": 1,
  "status": "PRESENT",
  "remark": "已签到"
}
```

---

### 2. 学生端签到

| 接口路径 | 请求方式 | 说明 | 权限要求 |
|---------|---------|------|---------|
| `/api/student/attendance/my-records` | GET | 查询学生自己的签到记录 | STUDENT |

---

### 3. 二维码签到

| 接口路径 | 请求方式 | 说明 | 权限要求 |
|---------|---------|------|---------|
| `/api/qr/teacher` | GET | 获取签到二维码（根据班级实验 ID） | TEACHER |
| `/api/qr/teacher/by-class-experiment` | GET | 根据班级代码和实验 ID 获取二维码 | TEACHER |
| `/api/qr/student/scan/{key}` | GET | 学生扫码签到 | STUDENT |

**获取二维码参数：**
```
classExperimentId=1
```

**根据班级和实验获取二维码参数：**
```
classCode=CS202301&experimentId=EXP001
```

---

## 四、实验管理模块（待实现）

### 1. 实验 CRUD

| 接口路径 | 请求方式 | 说明 | 权限要求 |
|---------|---------|------|---------|
| `/api/teacher/experiment` | POST | 创建实验 | TEACHER |
| `/api/teacher/experiment/{id}` | GET | 查询实验详情 | TEACHER |
| `/api/teacher/experiment/{id}` | PUT | 更新实验信息 | TEACHER |
| `/api/teacher/experiment/{id}` | DELETE | 删除实验 | TEACHER |
| `/api/teacher/experiment/list` | GET | 查询实验列表 | TEACHER |

**创建实验请求参数：**
```json
{
  "experimentId": "EXP001",
  "experimentName": "数据结构与算法实验1",
  "courseId": "CS001",
  "description": "链表的基本操作",
  "duration": 120
}
```

---

### 2. 实验步骤管理

| 接口路径 | 请求方式 | 说明 | 权限要求 |
|---------|---------|------|---------|
| `/api/teacher/experiment/{experimentId}/procedures` | POST | 添加实验步骤 | TEACHER |
| `/api/teacher/experiment/procedures/{procedureId}` | PUT | 更新实验步骤 | TEACHER |
| `/api/teacher/experiment/procedures/{procedureId}` | DELETE | 删除实验步骤 | TEACHER |

**添加实验步骤请求参数：**
```json
{
  "experimentId": "EXP001",
  "stepOrder": 1,
  "title": "创建链表结构",
  "content": "实现单链表的基本结构",
  "estimatedTime": 30
}
```

---

### 3. 学生实验进度

| 接口路径 | 请求方式 | 说明 | 权限要求 |
|---------|---------|------|---------|
| `/api/student/experiment/list` | GET | 查询学生的实验列表 | STUDENT |
| `/api/student/experiment/{experimentId}/procedures` | GET | 查询实验步骤 | STUDENT |
| `/api/student/experiment/procedure/{procedureId}/submit` | POST | 提交实验步骤 | STUDENT |

---

## 五、数据采集模块（待实现）

### 1. 学生提交数据

| 接口路径 | 请求方式 | 说明 | 权限要求 |
|---------|---------|------|---------|
| `/api/student/data/answer/submit` | POST | 提交实验答案 | STUDENT |
| `/api/student/data/file/upload` | POST | 上传实验文件 | STUDENT |
| `/api/student/data/photo/upload` | POST | 上传实验照片 | STUDENT |

**提交答案请求参数：**
```json
{
  "procedureTopicId": 1,
  "answer": "这是我的答案内容"
}
```

**上传文件参数：**
```
MultipartFile file
procedureId: 1
```

---

### 2. 教师查看数据

| 接口路径 | 请求方式 | 说明 | 权限要求 |
|---------|---------|------|---------|
| `/api/teacher/data/answers` | GET | 查看学生答案 | TEACHER |
| `/api/teacher/data/files` | GET | 查看学生文件 | TEACHER |
| `/api/teacher/data/photos` | GET | 查看学生照片 | TEACHER |

---

## 六、题目和标签管理模块（待实现）

### 1. 题目管理

| 接口路径 | 请求方式 | 说明 | 权限要求 |
|---------|---------|------|---------|
| `/api/teacher/topic` | POST | 创建题目 | TEACHER |
| `/api/teacher/topic/{id}` | GET | 查询题目详情 | TEACHER |
| `/api/teacher/topic/{id}` | PUT | 更新题目 | TEACHER |
| `/api/teacher/topic/{id}` | DELETE | 删除题目 | TEACHER |
| `/api/teacher/topic/list` | GET | 查询题目列表 | TEACHER |

**创建题目请求参数：**
```json
{
  "title": "链表的基本概念",
  "content": "什么是链表？",
  "type": "ESSAY",
  "difficulty": "EASY"
}
```

---

### 2. 标签管理

| 接口路径 | 请求方式 | 说明 | 权限要求 |
|---------|---------|------|---------|
| `/api/teacher/tag` | POST | 创建标签 | TEACHER |
| `/api/teacher/tag/list` | GET | 查询标签列表 | TEACHER |
| `/api/teacher/topic/{topicId}/tags` | POST | 为题目添加标签 | TEACHER |

**创建标签请求参数：**
```json
{
  "tagName": "链表",
  "description": "链表相关题目"
}
```

---

### 3. 题目与步骤绑定

| 接口路径 | 请求方式 | 说明 | 权限要求 |
|---------|---------|------|---------|
| `/api/teacher/procedure/{procedureId}/topics` | POST | 为实验步骤添加题目 | TEACHER |
| `/api/teacher/procedure/{procedureId}/topics` | GET | 查询实验步骤的题目 | TEACHER |

**添加题目到步骤请求参数：**
```json
{
  "topicIds": [1, 2, 3],
  "required": true
}
```

---

## 七、课程管理模块（待实现）

| 接口路径 | 请求方式 | 说明 | 权限要求 |
|---------|---------|------|---------|
| `/api/teacher/course` | POST | 创建课程 | TEACHER |
| `/api/teacher/course/{id}` | GET | 查询课程详情 | TEACHER |
| `/api/teacher/course/{id}` | PUT | 更新课程信息 | TEACHER |
| `/api/teacher/course/{id}` | DELETE | 删除课程 | TEACHER |
| `/api/teacher/course/list` | GET | 查询课程列表 | TEACHER |

**创建课程请求参数：**
```json
{
  "courseId": "CS001",
  "courseName": "数据结构与算法",
  "description": "计算机专业核心课程",
  "credits": 4
}
```

---

## 八、成绩管理模块（待实现）

### 1. 成绩录入

| 接口路径 | 请求方式 | 说明 | 权限要求 |
|---------|---------|------|---------|
| `/api/teacher/grade/input` | POST | 录入学生实验成绩 | TEACHER |
| `/api/teacher/grade/batch-input` | POST | 批量录入成绩 | TEACHER |

**录入成绩请求参数：**
```json
{
  "studentUsername": "2021001",
  "experimentId": "EXP001",
  "score": 95,
  "comment": "实验完成质量优秀"
}
```

**批量录入成绩请求参数：**
```json
{
  "grades": [
    {
      "studentUsername": "2021001",
      "experimentId": "EXP001",
      "score": 95
    },
    {
      "studentUsername": "2021002",
      "experimentId": "EXP001",
      "score": 88
    }
  ]
}
```

---

### 2. 成绩查询

| 接口路径 | 请求方式 | 说明 | 权限要求 |
|---------|---------|------|---------|
| `/api/teacher/grade/{classCode}/{experimentId}` | GET | 查询班级实验成绩 | TEACHER |
| `/api/student/grade/my-grades` | GET | 学生查询自己的成绩 | STUDENT |

---

## 九、数据导入导出模块

| 接口路径 | 请求方式 | 说明 | 权限要求 |
|---------|---------|------|---------|
| `/api/test/excel/import/users` | POST | 批量导入用户 | ADMIN |
| `/api/test/excel/template/users` | GET | 下载用户导入模板 | ADMIN |
| `/api/teacher/excel/export/attendance` | GET | 导出签到记录 | TEACHER |
| `/api/teacher/excel/export/grades` | GET | 导出成绩单 | TEACHER |

**用户导入模板格式：**

| 用户名 | 姓名 | 角色 | 学院 | 专业 |
|-------|------|------|------|------|
| 2021001 | 张三 | 学生 | 计算机学院 | 计算机科学与技术 |
| 2021002 | 李四 | 学生 | 计算机学院 | 软件工程 |
| T001 | 王老师 | 教师 | 计算机学院 | - |

---

## 通用响应格式

### 成功响应
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {}
}
```

### 失败响应
```json
{
  "code": 400,
  "message": "错误信息",
  "data": null
}
```

### 常见状态码
- `200`：操作成功
- `400`：请求参数错误
- `401`：未登录或 Token 无效
- `403`：无权限访问
- `404`：资源不存在
- `500`：服务器内部错误

---

## 权限说明

系统支持三种角色：
- **STUDENT**：学生
- **TEACHER**：教师
- **ADMIN**：管理员

所有接口（除登录外）都需要在请求头中携带 JWT Token：

```
Authorization: Bearer {token}
```

---

## 当前实现状态

### ✅ 已实现模块
- 用户认证（登录、密码管理、微信集成）
- 班级管理（创建、查询、学生绑定）
- 签到管理（二维码签到、签到记录查询）
- 二维码生成
- Excel 导入导出

### ⚠️ 待实现模块
- 实验管理
- 实验步骤管理
- 学生实验进度跟踪
- 数据采集（答案、文件、照片）
- 题目和标签管理
- 课程管理
- 成绩管理

---

## 技术栈

- **后端框架**：Spring Boot 3.5.5
- **安全认证**：JWT Token + Spring Security
- **数据库**：MySQL + MyBatis Plus
- **文档工具**：自动建表（AutoTable）
- **微信集成**：微信公众号 API
- **文件处理**：EasyExcel、ZXing（二维码）

---

**最后更新时间**：2026-01-26
